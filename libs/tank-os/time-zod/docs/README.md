# Time Zod adapter

Physical Zod validation boundary for `@tank-os/time`. Schemas depend on the
Time ports and are published separately so consumers that do not use Zod do
not acquire it through the primary package.
