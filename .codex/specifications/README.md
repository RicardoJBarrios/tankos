# Specifications

This directory is the canonical home for one-use-case specifications. A filename
or title must state whether its content is a candidate or accepted.

Each specification contains only actor, objective, preconditions, main flow,
variants, expected errors, domain events and acceptance criteria. It must not
redefine architecture or duplicate an ADR. Candidate specifications organize
discovery; only accepted ones authorize implementation.
