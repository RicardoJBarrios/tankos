# Time Firestore adapter

Physical Firebase Firestore boundary for `@tank-os/time`. It owns Firestore
`Timestamp` conversion and persistence-specific temporal representations. The
primary Time package remains free of Firebase dependencies.
