# @tank-os/units-firestore

Firestore persistence adapter for the independent TankOS units and conversion
catalogues. Firebase is intentionally absent from `@tank-os/units` itself.

## Running unit tests

Run `pnpm nx run units-firestore:test` to execute the tests with the 100%
coverage threshold. Build with `pnpm nx run units-firestore:build`.
